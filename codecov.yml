coverage:
  precision: 2
  round: nearest
  range: "80...100"
  status:
    project:
      default:
        threshold: 5%
    patch:
      default:
        target: 90%

  ignore:
    - "tests/**"


codecov:
  branch: master

comment:
  layout: "diff"
